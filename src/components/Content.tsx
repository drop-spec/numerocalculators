import Link from "next/link";
import { calculators, categories, type Calculator, type Category } from "@/lib/calculators";
export function Breadcrumbs({ calculator }: { calculator: Calculator }) { const href=calculator.ai?"/ai-calculators":`/${calculator.category}`; return <nav className="crumbs" aria-label="Breadcrumb"><Link href="/">Home</Link><span>/</span><Link href={href}>{categories[calculator.category].accent}</Link><span>/</span><span>{calculator.title}</span></nav>; }
export function Related({ calculator }: { calculator: Calculator }) { const relevant=calculators.filter(x=>x.slug!==calculator.slug && x.category===calculator.category).slice(0,3); return <section className="related"><h2>Related calculators</h2><div className="related-links">{relevant.map(item=><Link href={`/${item.ai?"ai-calculators":"calculators"}/${item.slug}`} key={item.slug}>{item.title}<span>→</span></Link>)}</div></section>; }
export function CategoryLinks({ current }: { current?: Category }) { return <div className="category-links">{(Object.keys(categories) as Category[]).filter(key=>key!==current).map(key=><Link key={key} href={`/${key}`}>{categories[key].accent}</Link>)}</div>; }
